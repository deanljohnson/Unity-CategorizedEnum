using UnityEngine;

namespace Assets.Plugins.CategorizedEnum
{
    /// <summary>
    /// Specifies that an enum field should display 
    /// with a categorized enum popup. Use this to categorize
    /// a property of enum type with Category attributes that
    /// is not categorized by default.
    /// </summary>
    public class CategorizedAttribute : PropertyAttribute
    {
    }
}
